---
interface Props {
  title?: string;
  body?: string;
}

const { title, body } = Astro.props;
---

<>
  {title && <h2 class="text-lg sm:text-xl font-bold m-2">{title}</h2>}
  {body && <p class="m-2 font-light text-base">{body}</p>}
  <slot />
</>
